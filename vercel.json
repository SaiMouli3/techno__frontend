{
    "build": {
        "env": {
            "FORCE_LEGACY_PEER_DEPS": "true"
        }
    },
    "functions": {
        "preBuild": {
            "command": "npm install --legacy-peer-deps"
        }
    }
}